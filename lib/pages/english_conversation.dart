import 'package:flutter/material.dart';

class EnglishConversationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('英会話集')),
      body: Center(child: Text('英会話集のページ')),
    );
  }
}
