import 'package:flutter/material.dart';

class FamilyListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('家族リスト')),
      body: Center(child: Text('家族リストのページ')),
    );
  }
}
