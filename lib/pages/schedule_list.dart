import 'package:flutter/material.dart';

class ScheduleListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('予定表一覧')),
      body: Center(child: Text('予定表一覧のページ')),
    );
  }
}
