import 'package:flutter/material.dart';

class ItemsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('持ち物')),
      body: Center(child: Text('持ち物ページ')),
    );
  }
}
